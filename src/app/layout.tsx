import type { Metadata } from 'next';import './globals.css';import { Header } from '@/components/layout/Header';import { Footer } from '@/components/layout/Footer';
export const metadata: Metadata={title:'Exclusive Media | Digital Growth Architecture',description:'Exclusive Media connects advertising, websites, CRM, automated follow-up, booking, and reputation into one growth operating system for established service businesses.',openGraph:{title:'Exclusive Media | Digital Growth Architecture',description:'One connected growth system instead of disconnected agencies, tools, campaigns, and handoffs.',type:'website'}};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><Header/><main>{children}</main><Footer/></body></html>}
