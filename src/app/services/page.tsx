import { stages } from '@/content/site';import { CTAButton } from '@/components/ui/CTAButton';import { SectionHeading } from '@/components/ui/SectionHeading';
const engines=['Visibility','Authority','Conversion','Nurture & Sales','Customer Experience','Retention & Expansion','Intelligence'];
export default function Page(){return <section className="grid-bg px-4 py-20"><SectionHeading eyebrow="Services / Growth OS" title="Build the connected operating system behind growth" body="ATTRACT, CONVERT, SERVE, and GROW are the public stages. Seven internal engines keep the architecture working."/><div className="mx-auto grid max-w-7xl gap-6 md:grid-cols-4">{stages.map(s=><article className="card rounded-3xl p-6" key={s.k}><h2 className="text-2xl font-black text-teal">{s.k}</h2><p className="mt-3 text-mist">{s.x}</p></article>)}</div><div className="mx-auto mt-10 max-w-6xl"><h2 className="text-3xl font-black">Seven internal engines</h2><div className="mt-5 grid gap-3 md:grid-cols-7">{engines.map(e=><div className="rounded-2xl border border-white/10 bg-white/5 p-4 text-sm" key={e}>{e}</div>)}</div><p className="mt-8 text-mist">Out of scope unless separately scoped: custom native software development, direct ad spend, custom API engineering outside standard platforms, and client legal/compliance fees.</p><div className="mt-6"><CTAButton href="/contact">Discuss Buildout</CTAButton></div></div></section>}
