import Link from 'next/link';import { site } from '@/content/site';import { CTAButton } from '@/components/ui/CTAButton';
export function Header(){return <><div className="border-b border-white/10 bg-teal/10 px-4 py-2 text-center text-xs text-mist">{site.alert}</div><header className="sticky top-0 z-40 border-b border-white/10 bg-charcoal/85 backdrop-blur"><nav className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link href="/" className="focus-ring"><span className="block text-lg font-black">{site.name}</span><span className="text-xs uppercase tracking-[.25em] text-gold">{site.descriptor}</span></Link><div className="hidden gap-5 text-sm text-mist lg:flex">{site.nav.map(([l,h])=><Link className="hover:text-teal" key={l} href={h}>{l}</Link>)}</div><CTAButton>Calculate Leaks</CTAButton></nav></header></>}
