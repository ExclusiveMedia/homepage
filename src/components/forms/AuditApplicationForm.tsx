'use client';import { useState } from 'react';
const fields=['Name','Business email','Phone','Company name','Website URL','Industry','Annual revenue range','Monthly marketing spend','Current CRM/software','Average lead response time','Biggest growth leak','Decision-maker status','Preferred next step'];
export function AuditApplicationForm(){const[submitted,setSubmitted]=useState(false);if(submitted)return <div className="card rounded-3xl p-8"><h2 className="text-3xl font-black">Application stub received</h2><p className="mt-3 text-mist">This demo prevents live submission until approved CRM/webhook, consent storage, notification, and booking integrations are configured.</p></div>;return <form className="card rounded-3xl p-6" onSubmit={e=>{e.preventDefault();setSubmitted(true)}}>{/* TODO: Add hidden UTM fields, GHL/Zapier/Make webhook, CRM mapping, TCPA consent storage, STOP/HELP automation, 10DLC approval, email alerts, and booking integration. */}<div className="grid gap-4 md:grid-cols-2">{fields.map(f=><label className="text-sm" key={f}>{f}<input required className="mt-2 w-full rounded-xl border border-white/10 bg-black p-3" placeholder={f}/></label>)}</div><label className="mt-5 flex gap-3 text-sm text-mist"><input type="checkbox" className="mt-1"/>I agree to receive calls/texts about my request. Consent is not a condition of purchase. Message/data rates may apply. Reply STOP to opt out and HELP for help. Legal must approve final TCPA language.</label><p className="mt-3 text-xs text-mist/60">Privacy disclaimer: submission handling is disabled in this stub. Attorney review required before launch.</p><button className="focus-ring mt-6 rounded-full bg-teal px-6 py-3 font-bold text-charcoal">Submit Application Stub</button></form>}
