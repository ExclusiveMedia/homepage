'use client';import { useState } from 'react';import { CTAButton } from '@/components/ui/CTAButton';
const steps=[['Annual revenue','Industry','Decision-maker status'],['Current CRM/software','Average lead response time','Monthly marketing spend'],['Lead volume','Booking process','Review generation'],['Attribution clarity','Current vendors','Biggest bottleneck']];
export function ScorecardForm(){const[step,setStep]=useState(0),[done,setDone]=useState(false);if(done)return <div className="card rounded-3xl p-8"><p className="text-teal">Mock result screen</p><h2 className="mt-2 text-4xl font-black">Growth System Score: 63/100</h2><p className="mt-4 text-mist">Primary leak: SERVE — response time, CRM handoff, and booking automation. Recommended next step: apply for a Growth Architecture Audit.</p><div className="mt-6"><CTAButton href="/contact">Apply for Audit</CTAButton></div></div>;return <form className="card rounded-3xl p-6" onSubmit={e=>{e.preventDefault(); step<steps.length-1?setStep(step+1):setDone(true)}}>{/* TODO: Connect to GHL/HubSpot/Zapier/Make webhook, CRM field mapping, UTM capture, TCPA consent storage, 10DLC confirmation, email notification, and calendar booking integration. */}<p className="text-sm text-teal">Step {step+1} of {steps.length}</p><div className="mt-5 grid gap-4 md:grid-cols-3">{steps[step].map(q=><label key={q} className="text-sm">{q}<input required className="mt-2 w-full rounded-xl border border-white/10 bg-black p-3" placeholder="Your answer"/></label>)}</div><button className="focus-ring mt-6 rounded-full bg-teal px-6 py-3 font-bold text-charcoal">{step<steps.length-1?'Continue':'View My Score'}</button><p className="mt-3 text-xs text-mist/60">No live submission occurs until backend, consent, webhook, and compliance requirements are configured.</p></form>}
