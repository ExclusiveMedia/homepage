'use client';import { useState } from 'react';import { stages } from '@/content/site';
export function StageStepper(){const[i,setI]=useState(0),s=stages[i];return <div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><div className="grid gap-3">{stages.map((x,idx)=><button key={x.k} onClick={()=>setI(idx)} className={`focus-ring rounded-2xl border p-4 text-left ${i===idx?'border-teal bg-teal/10':'border-white/10 bg-white/5'}`}><span className="text-xs text-gold">Stage 0{idx+1}</span><b className="block">{x.k}</b></button>)}</div><div className="card rounded-3xl p-8"><p className="text-gold">{s.m}</p><h3 className="mt-2 text-3xl font-black">{s.k} — {s.t}</h3><p className="mt-4 text-mist">{s.x}</p><p className="mt-4 font-bold text-teal">Business benefit: {s.b}</p><ul className="mt-5 grid gap-2">{s.d.map(d=><li key={d}>• {d}</li>)}</ul></div></div>}
